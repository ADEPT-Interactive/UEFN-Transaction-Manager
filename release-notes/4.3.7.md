- Restored ordinary catalog create, edit, duplicate, and nested alternate-offer CRUD while preserving every existing public Verse identity; identity changes remain explicit migration-only operations.
- Added explicit mutation DTO serialization so hydrated icon previews, file-picker metadata, and echoed identity hints do not cross the bridge, while dynamic pricing and quantity behavior remain intact.
- Replaced the generic bridge failure with stable oversized-payload, invalid-JSON, and renderer-safe internal-error responses backed by a local diagnostic reference.
- Added installed-package CRUD coverage for temporary entitlements and alternate offers, migrated edits, UTM-native edits, duplicate cleanup, and atomic identity-change rejection.
