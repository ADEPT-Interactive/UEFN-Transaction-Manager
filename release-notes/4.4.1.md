- Fixed entitlement Duplicate so the real renderer awaits the bridge create operation, surfaces failures, preserves the original, and renders exactly one fresh-identity copy; bundle duplication now follows the same awaited mutation boundary.
- Migrated generated Verse for UEFN 42.20 to `/UnrealEngine.com/Marketplace` while retaining backward loading of old managed files and preserving catalog/public identity semantics.
- Centralized runtime-price validation into one exact shared 50-to-5000 V-Bucks validator, expanded boundary coverage, and added the opt-in durable Ownership Confirmed reconciliation trigger without changing purchase Success Triggers or the existing Verse event APIs.
- Reused the V-Bucks slider, numeric input, and presets across offers, alternates, and bundles; corrected bundle pointer hitboxes and full-width icon layout; and added alternate upload/import/adoption preview and persistence coverage.
- Corrected fresh exact connector precedence over stale 42.20 lifecycle opening text with fail-closed project/PID identity checks, plus unit, contract, renderer, Electron/package, and native UTM_Demo compile/BuildAll verification.
